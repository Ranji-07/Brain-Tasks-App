apiVersion: v1
kind: Service
metadata:
  name: brain-tasks-service
  namespace: brain-tasks
spec:
  selector:
    app: brain-tasks
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
